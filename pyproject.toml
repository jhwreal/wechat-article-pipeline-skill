[project]
name = "wechat-article-pipeline-skill"
version = "1.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["wechat-article-pipeline/scripts"]
