# 更新记录

每次发布在这里记录行为变化、验证结果和兼容性说明。具体实现可通过对应 commit 的标题、正文和 diff 追踪；已发布的 tag 保留不移动。

## v1.9.1 — 2026-09-21

维护版本；保留由“检查”或“检查一下”触发的检查模式。完整问题、提交映射和回退方式见 [v1.9.1 发布说明](docs/releases/v1.9.1.md)。

### 修复

- A01：工作台 HTTP 入口绑定当前文章；其他文章不再通过同一服务进入编辑，保存和图片上传核对文章身份。
- A02：缓存恢复核对原始文章、版本及内容指纹，冲突稿保留供对照；多个保存服务在文件锁内重读状态，拒绝旧版本覆盖。
- A03：token 缓存按实际 AppID 定位并校验归属，旧的无身份缓存和其他账号缓存不会直接复用。
- A04：微信列表转换保留续行和嵌套子项；Python 发布与 JavaScript 预览共用 7 组内容保真样例。
- A05：托管图片回执绑定当前正文及图片内容；同数量换图、原路径换图和图片重排使旧回执失效。无法核实的复制使用内嵌图片。
- A06：微信 dry-run 和新建草稿入口核对当前任务数据、素材及工作台版本，拒绝旧清单；发布恢复仍依据原运行记录处理已创建的草稿。
- A07：三平台状态绑定正文及本地图片版本，改稿后保留上一轮历史；结果合并核对来源和验收计数，创建成功不再等同于回读验证成功，未知提交标记不能被普通失败记录清除。
- A08：同篇改稿从当前文章已保存的签名读取篇号，验证文章与账号归属；另一篇文章推进账号总篇号后，原文章仍保持自己的篇号。
- A09：标题中的反斜杠、正则替换符号和 HTML 特殊字符按字面处理，避免构建和保存异常。

### 可维护性与检查模式

- 将文章保存、事务和版本模型独立放入 `workbench_document.py`；`serve_wechat_workbench.py` 专注 HTTP 入口，保留原有导入接口。
- 将 Markdown 解析移至 `assets/workbench-markdown.js`，打包时仍内嵌到单文件工作台；发布清单候选文件按服务实例隔离。
- 图片回执写回与浏览器保存共用文章文件锁；旧缓存的正文及样式都经版本核对后才恢复，冲突稿在后续刷新时仍可查看。
- 检查报告记录被检查原文的 SHA-256、检查时间和可用的工作台版本；增加 6 组人工行为验收案例，覆盖检查范围、事实边界、直接改稿与触发歧义。

### 已执行的验证

- 全量自动测试通过：仓库 Python 162 项、安装包内 Python 30 项、JavaScript 51 项，共 243 项。
- Skill 结构、编译、版本元数据和发布脚本语法检查通过；安装副本通过 `doctor --require-installed-sync`，本机账号配置和篇号保持不变。
- Chrome 实际操作通过：保存含反斜杠标题及列表续行的文章；另一窗口保存新稿后，旧窗口保存被拒绝；刷新显示最新稿并可展开复制保留的冲突稿。
- 自动测试与浏览器验收使用临时文章、本地 HTTP、模拟回执及 API dry-run，没有调用真实平台发布接口。6 组人工行为案例是后续检查模式验收素材，不计入自动测试通过数。

### 文档勘误

- 2026-09-21：补齐中英文 README 和发布说明中的头条图片降级描述：有效回执缺失时内嵌原图，原图读取失败才停止复制。以独立文档提交保留修正记录，`v1.9.1` 标签及安装包代码不变。
- 增加说明同步步骤：`main` 测试通过且技能源码与该版本标签完全相同时，更新现有 GitHub Release 说明，保留原安装包；包含运行代码变更时跳过，等待新版本发布。

### 兼容性

- 新服务要求保存请求携带文章身份和原始版本。旧工作台应从当前 Markdown 和 job 重新打包后编辑；保留旧浏览器草稿用于对照，不自动覆盖当前文件。
- 无来源绑定的旧图片回执会回退内嵌图片；旧发布清单若无法与当前文章核对，需要重新生成。Markdown 中直接引用的本地图片也参与发布指纹。
- 旧三平台状态先运行 `init` 迁移，原记录保存在 `history`。平台结果需携带本轮版本指纹和实际验收计数。同篇改稿缺少原文章签名时会停止，需恢复真实记录，不能倒推或回退账号总篇号。
- 修复前基准为 `v1.9.0`（`bd54ac3`）。回退以新增 revert 提交或独立检出旧 tag 为准，不移动旧 tag，也不覆盖本机账号凭据和文章文件。
